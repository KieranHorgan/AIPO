#include <bits/stdc++.h>

#define ll long long

using namespace std;

ll n, m, q, k, ans;
vector<int> a;
string s;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cin >> n;

	cout << ans << endl;
}